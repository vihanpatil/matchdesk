packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 11 requires explicit approval before a dependency may run install scripts.
# better-sqlite3 compiles a native addon (or downloads a prebuild); esbuild
# fetches its platform binary. Both are build-time only and expected.
allowBuilds:
  better-sqlite3: true
  esbuild: true
